matrix:
  - stack: abacus+librpa
    status: draft
    notes: Start with smoke-ready static checks; refine by real cases.
    versions:
      abacus: ["3.x"]
      librpa: ["0.x"]
    constraints:
      - Inputs must come from one workflow chain
      - Run directory must be isolated and newly created
